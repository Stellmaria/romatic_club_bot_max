name: Apply issue 30 CI fixes

on:
  push:
    branches: [refactor/issue-30-application-use-cases]
    paths:
      - '.issue30-ci-fix.patch'

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: refactor/issue-30-application-use-cases
      - name: Apply CI fixes
        shell: bash
        run: |
          set -euo pipefail
          git apply --check .issue30-ci-fix.patch
          git apply .issue30-ci-fix.patch
          rm .issue30-ci-fix.patch .github/workflows/issue30-ci-fix-apply.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -- \
            bot/handlers/admin/uid_verification_admin.py \
            bot/handlers/auction/exchange/submission.py \
            bot/use_cases/exchange_submission.py \
            .issue30-ci-fix.patch \
            .github/workflows/issue30-ci-fix-apply.yml
          git diff --cached --check
          git commit -m "fix(ci): preserve issue 30 compatibility contracts"
          git push origin HEAD:refactor/issue-30-application-use-cases
