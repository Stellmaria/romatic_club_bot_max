# Рефакторинг: этап 1

## Выполнено

- Добавлен единый менеджер фоновых задач `bot/core/tasks.py`.
- Подключены забытые фоновые процессы UID-верификации и синхронизации Luxury-статуса.
- Удалена безусловная повторная регистрация debug middleware.
- Удалён устаревший обработчик `/profile`; оставлена версия со статусом UID.
- Команда `/autobid_set` переведена на единый helper определения шага валюты.
- Созданы архитектурные пакеты `bot/core`, `bot/services`, `bot/repositories`.
- `requirements.txt` переведён из UTF-16 в UTF-8.
- Удалён не относящийся к проекту forensic/dissect-набор зависимостей.
- Добавлены отсутствующие зависимости `pandas` и `pytz`.
- Добавлены безопасные `.gitignore` и `.env.example`.
- Добавлены статические регрессионные тесты.

## Проверки

- `python -m compileall` проходит.
- 3 статических теста проходят.

## Следующий этап

1. Разделить UID verification на router/service/repository.
2. Удалить конфликтующие callback handlers.
3. Сделать завершение аукциона транзакционным и идемпотентным.
4. Разделить `auctions.py` на доменные роутеры.
5. Перенести SQL из `db/db.py` в repositories.
6. Ввести Alembic и версионирование схемы.
